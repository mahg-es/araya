# 006 — Pi 0.82.1 Runtime — Verified State — v2
> Capsule ID: 006 | Initiative: pi-082-compatibility | Status: VERIFIED (2026-07-26)
> Session: 2026-07-26-v2 | Verified on: pi 0.82.1 (`pi --version`), framework dev-mahg 38197e6

| Component | State | Evidence |
|---|---|---|
| pi binary | 0.82.1 operational | `pi --version` → 0.82.1; `settings.json lastChangelogVersion: 0.82.1` |
| `~/.pi/agent/tools/` | ABSENT (retired layout) | `ls` → No such file or directory |
| `~/.pi/agent/extensions/` | loadable, active | `araya/index.ts`, `araya.ts`, `araya-notifier.ts`, `runtime-model-context.ts`, `subagent/` (agents.ts, index.ts); session model identity supplied via ExtensionContext |
| `~/.pi/agent/libexec/araya/` | 4 Python helpers | `ax_audit.py`, `loop_silence_guard.py`, `postoffice_loop.py`, `session_identity.py` |
| Skill frontmatter contract | enforced | `tests/skill-frontmatter-test.js`: every `skills/*/SKILL.md` requires name+description+non-empty body; missing description FAILS; 637 checks exit 0 (PR #82) |
| `relay-participant` skill | valid | frontmatter added (PR #82); appears in Pi session skill list; user/global copy byte-identical to repo copy (pre-merge verification) |
| `.pi/agents/` (repo) | 30/30 tracked, regenerated | generator v2.1.0; source-hash headers; `--check` no drift at 38197e6 |
| `.araya/generated/` | 120 profiles (pi/codex/claude-cli/agy) | regenerated by canonical generator; `--check` exit 0 |
| Skill name collisions | none active | repo `skills/` mirrors to `~/.pi/agent/skills/araya/` by design; prior frontmatter conflict resolved at source |

## Install/sync rule (canonical)
The repository is the corrected source. Runtime copies under `~/.pi/agent/` are install-time copies — never edit them manually; re-sync from the repo after merges that touch `skills/`, `prompts/`, or `araya.yaml`. Documented sync path: copy `skills/<name>/SKILL.md` → `~/.pi/agent/skills/araya/<name>/SKILL.md` (or rerun the project's installer when one exists).

## Runtime model identity (this session)
Provider `moonshotai`, model `kimi-k3` — supplied by Pi runtime (ExtensionContext.ctx.model). Gate agents (PR #294, PR #82) ran `deepseek-v4-pro` (provider deepseek) per their own declarations.

> v2 sources: direct command verification 2026-07-26, PR #82 test logs, settings.json.
